import React from 'react';

const PhoneEdition = ({contacts}) =>  (
        <>
        {/* <ul>
            {
                contacts.map(contact =>(
                    <li key={contact.id}>
                        <p>{contact.text}</p>
                        <button type="button">Editor</button>
                    </li>
                )           
                )
            }
        </ul> */}
        </>
    );


export default PhoneEdition;